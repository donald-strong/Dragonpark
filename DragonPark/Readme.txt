Dragon Parking
==============

$ mvn install

Not quite up and running but unit tests and integration tests run sucessfully.

Uses an H2 in memory database.

schema.sql creates the database.
data.sql populates it.

Look at RatesTestEngine.java to get a good idea of how it all fits together.

RepositoryTest.java is an integration test that proves the Spring configuration. 


